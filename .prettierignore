node_modules
dist
coverage
.yarn

*.lock

consensus

# Thales workspace artifacts. The on-disk Thales clone lives here when
# verify.sh has been run locally; CI doesn't have it (only the
# verify_core job creates one, in its own checkout) but excluding it
# keeps `yarn format:check` consistent across environments.
packages/core/.thales/

# Thales-emitted Lean sidecars. Regenerated by verify.sh; not committed.
packages/core/Generated/*.lean
