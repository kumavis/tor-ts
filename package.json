{
  "private": true,
  "name": "tor-ts-workspace",
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "eslint": "^9",
    "eslint-config-prettier": "^9",
    "eslint-import-resolver-typescript": "^3",
    "eslint-plugin-import": "^2",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "yarn workspace tor dev",
    "test": "yarn workspace tor test",
    "typecheck": "yarn workspace tor typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --ext .ts,.js,.cjs",
    "lint:fix": "yarn lint --fix",
    "import:check": "yarn workspace tor import:check",
    "test:dev": "yarn workspace tor test:dev"
  }
}
